# 版本对比工具# 版本对比工具# Zeus Editor & Game

一个基于 Electron 的版本对比工具，用于比较不同版本之间的数据差异，提供详细的对比结果和导出功能。一个基于 Electron 的版本对比工具，用于比较不同版本之间的数据差异，提供详细的对比结果和导出功能。一个基于 Electron 的多功能桌面应用程序，包含文本编辑器和 2D 游戏功能。

## 主要功能## 主要功能## 运行应用

- 🔍 **版本检测**: 自动扫描并检测可用的版本目录- 🔍 **版本检测**: 自动扫描并检测可用的版本目录### 开发模式

- 📊 **智能对比**: 对比两个版本之间的数据差异

- 📈 **详细分析**: 显示新增、修改、删除的详细信息- 📊 **智能对比**: 对比两个版本之间的数据差异

- 💾 **导出功能**: 支持导出 TXT 和 Excel 格式的对比结果

- 🎯 **CardID 导出**: 专门的 CardID 导出功能- 📈 **详细分析**: 显示新增、修改、删除的详细信息\`\`\`bash

- 🖥️ **简洁界面**: 清爽的光色主题界面

- 💾 **导出功能**: 支持导出 TXT 和 Excel 格式的对比结果npm start

## 运行应用

- 🎯 **CardID 导出**: 专门的 CardID 导出功能\`\`\`

### 开发模式

- 🖥️ **简洁界面**: 清爽的光色主题界面

```bash

npm start### 打包应用

```

## 运行应用

### 打包应用

\`\`\`bash

```bash

npm run package### 开发模式npm run package

```

\`\`\`

### 构建安装包

````bash

```bash

npm run makenpm start### 构建安装包

````

```

## 项目结构

\`\`\`bash

```

zeus/### 打包应用npm run make

├── src/ # 源代码目录

│ ├── main/ # 主进程代码\`\`\`

│ │ └── main.js # Electron 主进程入口

│ ├── renderer/ # 渲染进程代码```bash

│ │ ├── index.html # 主页面

│ │ ├── version-compare.html # 版本对比页面 npm run package## 项目结构

│ │ ├── version-compare.js # 版本对比核心逻辑

│ │ └── version-compare.css # 样式文件```

│ └── preload/ # 预加载脚本

│ └── preload.js # 安全的API暴露\`\`\`

├── assets/ # 静态资源目录

├── config/ # 配置文件目录### 构建安装包zeus/

│ └── forge.config.js # Electron Forge 配置

├── data/ # 版本数据目录 (本地保留，不提交到 Git)├── index.js # 主进程文件

│ ├── version1/

│ ├── version2/````bash├── preload.js # 预加载脚本

│ └── ...

├── package.json # 项目配置npm run make├── index.html # 主界面

├── forge.config.js # Electron Forge 配置

└── README.md # 项目说明```├── styles.css # 样式文件

`````

├── renderer.js # 渲染进程逻辑

## 使用方法

## 使用方法├── package.json # 项目配置

1. **启动应用**: 运行 `npm start` 启动应用程序

2. **检测版本**: 点击"🔍 检测版本"按钮扫描可用版本├── forge.config.js # Electron Forge 配置

3. **选择版本**: 从下拉菜单中选择要对比的旧版本和新版本

4. **开始对比**: 点击"🔍 开始对比"按钮开始版本对比1. **启动应用**: 运行 `npm start` 启动应用程序└── assets/ # 资源文件

5. **查看结果**: 在结果页面查看详细的对比信息

6. **导出数据**: 使用导出功能保存对比结果2. **检测版本**: 点击"🔍 检测版本"按钮扫描可用版本



## 技术栈3. **选择版本**: 从下拉菜单中选择要对比的旧版本和新版本```



- **Electron**: 跨平台桌面应用框架4. **开始对比**: 点击"🔍 开始对比"按钮开始版本对比

- **HTML/CSS/JavaScript**: 前端技术

- **Node.js**: 后端文件处理5. **查看结果**: 在结果页面查看详细的对比信息## 技术栈

- **ExcelJS**: Excel 文件生成

6. **导出数据**: 使用导出功能保存对比结果

## 核心功能模块

- **Electron**: 跨平台桌面应用框架

### 1. 版本检测

- 自动扫描 `data/` 目录下的版本文件夹## 项目结构- **HTML/CSS/JavaScript**: 前端技术

- 验证版本数据的完整性

- 支持版本列表刷新- **Node.js**: 后端 API



### 2. 数据对比````

- 深度对比两个版本的 JSON 数据文件

- 识别新增、修改、删除的记录zeus/## 开发说明

- 提供详细的差异分析

├── index.js # 主进程文件

### 3. 结果展示

- 分类显示对比结果（新增/修改/删除）├── preload.js # 预加载脚本这个项目是一个很好的 Electron 学习示例，包含了：

- 支持结果筛选和搜索

- 提供统计信息摘要├── index.html # 主界面



### 4. 导出功能├── version-compare.js # 版本对比核心逻辑1. **主进程 (index.js)**:

- TXT 格式导出：简洁的文本格式

- Excel 格式导出：结构化的表格数据├── package.json # 项目配置 - 窗口管理

- CardID 专项导出：针对特定数据格式

├── forge.config.js # Electron Forge 配置 - 菜单创建

## 数据格式要求

├── .gitignore # Git 忽略文件配置 - 文件系统操作

版本数据应该存储在 `data/版本号/` 目录下，包含以下文件：

- `CARD.json`: 主要的卡牌数据文件└── data/ # 版本数据目录 (本地保留，不提交到 Git) - IPC 通信

- 其他相关的 JSON 数据文件

    ├── version1/

## 开发说明

    ├── version2/2. **渲染进程 (renderer.js)**:

这个项目展示了以下 Electron 开发实践：

    └── ...   - 用户界面逻辑

1. **主进程管理**: 窗口创建、菜单设置、文件系统操作

2. **渲染进程通信**: IPC 通信处理文件读写````- 编辑器功能

3. **文件系统操作**: 目录扫描、文件读写、路径处理

4. **用户界面**: 现代化的响应式设计   - 事件处理

5. **数据处理**: JSON 数据解析和对比算法

6. **错误处理**: 完善的错误捕获和用户提示## 技术栈

7. **安全性**: 启用 webSecurity 和 Content Security Policy

3. **预加载脚本 (preload.js)**:

## 项目架构

- **Electron**: 跨平台桌面应用框架   - 安全的 API 暴露

### 源代码组织

- **src/main/**: 主进程代码，负责应用程序生命周期管理- **HTML/CSS/JavaScript**: 前端技术   - 上下文隔离

- **src/renderer/**: 渲染进程代码，负责用户界面和业务逻辑

- **src/preload/**: 预加载脚本，安全地桥接主进程和渲染进程- **Node.js**: 后端文件处理



### 安全实践- **ExcelJS**: Excel 文件生成4. **样式设计 (styles.css)**:

- 禁用 nodeIntegration

- 启用 contextIsolation   - 现代化 UI

- 使用 preload 脚本安全地暴露 API

- 配置 Content Security Policy## 核心功能模块   - 响应式布局



## Git 配置   - VS Code 风格



项目已配置 `.gitignore` 文件，确保：### 1. 版本检测

- `data/` 目录不会被提交到 Git 仓库

- `node_modules/` 等依赖文件被忽略- 自动扫描 `data/` 目录下的版本文件夹## 扩展建议

- 构建输出目录被忽略

- 验证版本数据的完整性

这样可以避免大文件提交问题，同时保持本地数据完整性。

- 支持版本列表刷新你可以继续添加以下功能来学习更多 Electron 特性：

## 许可证



MIT License
### 2. 数据对比### 编辑器扩展：

- 深度对比两个版本的 JSON 数据文件- 语法高亮

- 识别新增、修改、删除的记录- 查找替换

- 提供详细的差异分析- 多标签页

- 插件系统

### 3. 结果展示- 主题切换

- 分类显示对比结果（新增/修改/删除）- 自动保存

- 支持结果筛选和搜索- 文件树视图

- 提供统计信息摘要

### 游戏扩展：

### 4. 导出功能- 更多游戏类型（贪吃蛇、俄罗斯方块等）

- TXT 格式导出：简洁的文本格式- 多人游戏支持

- Excel 格式导出：结构化的表格数据- 音效和背景音乐

- CardID 专项导出：针对特定数据格式- 成就系统

- 游戏排行榜

## 数据格式要求- 自定义皮肤



版本数据应该存储在 `data/版本号/` 目录下，包含以下文件：## 游戏开发技术要点

- `CARD.json`: 主要的卡牌数据文件

- 其他相关的 JSON 数据文件这个 2D 游戏展示了以下重要的游戏开发概念：



## 开发说明1. **游戏循环 (Game Loop)**: 使用 `requestAnimationFrame` 实现流畅的 60fps 游戏循环

2. **状态管理**: 游戏状态机（菜单、游戏中、暂停、游戏结束）

这个项目展示了以下 Electron 开发实践：3. **碰撞检测**: AABB（轴对齐包围盒）碰撞检测算法

4. **粒子系统**: 爆炸和升级特效的粒子系统

1. **主进程管理**: 窗口创建、菜单设置、文件系统操作5. **输入处理**: 键盘事件监听和多键同时按下处理

2. **渲染进程通信**: IPC 通信处理文件读写6. **Canvas 绘图**: HTML5 Canvas 2D 上下文绘图 API

3. **文件系统操作**: 目录扫描、文件读写、路径处理7. **动画和缓动**: 平滑的移动和视觉效果

4. **用户界面**: 现代化的响应式设计8. **数据持久化**: 使用 localStorage 保存最高分

5. **数据处理**: JSON 数据解析和对比算法

6. **错误处理**: 完善的错误捕获和用户提示这是一个很好的学习 Electron 和游戏开发的综合项目！



## Git 配置## 许可证



项目已配置 `.gitignore` 文件，确保：MIT License

- `data/` 目录不会被提交到 Git 仓库```

- `node_modules/` 等依赖文件被忽略
- 构建输出目录被忽略

这样可以避免大文件提交问题，同时保持本地数据完整性。

## 许可证

MIT License
`````
